-- A script that lists all the tables of 
-- a database in my MySQL server.
-- The database name will be passed as argument of mysql command
-- An example: cat 3-list_tables.sql | mysql -hlocalhost -uroot -p mysql
-- In the example below, mysql is the database name.
SHOW TABLES;
